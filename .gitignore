node_modules/

# Supabase CLI local state — regenerated by `supabase link` / `supabase start`.
supabase/.temp/
supabase/.branches/

# Secrets — never commit real keys.
.env
.env.local

# Self-hosting: deploy.sh clones the official Supabase repo here at deploy
# time — never commit a copy of it (and its generated .env with real secrets).
self-hosting/supabase-project/
